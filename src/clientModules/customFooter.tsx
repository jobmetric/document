// This module is no longer needed as we're using CustomFooter directly in pages
// Keeping it empty to avoid errors
export default function () {
  // Footer is now rendered directly in pages that need it
}

